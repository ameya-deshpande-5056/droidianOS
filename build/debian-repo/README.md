# Debian Repository

Use this area for local/staged APT repository generation.

Recommended production tool:

```text
aptly
```

Repository publishing must produce signed `Release`, `Release.gpg`, and `InRelease` metadata.
