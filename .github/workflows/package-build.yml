name: package-build

on:
  push:
  pull_request:

jobs:
  package-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update
      - run: sudo apt-get install -y build-essential cargo devscripts debhelper dpkg-dev libdbus-1-dev rustc
      - run: sh ./build/scripts/build-packages.sh
