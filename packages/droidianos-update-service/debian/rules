#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	cargo build --release --locked

override_dh_auto_install:
	install -Dm0755 target/release/droidianos-updated debian/droidianos-update-service/usr/bin/droidianos-updated
