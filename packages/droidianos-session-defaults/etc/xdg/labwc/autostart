#!/bin/sh

swaybg --image /usr/share/backgrounds/droidianos/droidianos-wallpaper.png --mode fill &
droidianos-arm-setup
if grep -qs 'boot=live' /proc/cmdline || [ -e /run/live/medium ]; then
  droidianos-installer &
else
  droidianos-desktop-chooser &
fi
waydroid session start &
