# Upgrade Tests

`upgrade-smoke.sh` verifies that a system can upgrade project packages and still run basic diagnostics.

Expected environment:

- installed droidianOS VM
- configured project APT repository
- passwordless sudo or CI-controlled sudo

